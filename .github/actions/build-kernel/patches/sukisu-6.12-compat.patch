diff --git a/kernel/kpm/super_access.c b/kernel/kpm/super_access.c
index f3261c9..d53a785 100644
--- a/kernel/kpm/super_access.c
+++ b/kernel/kpm/super_access.c
@@ -129,7 +129,9 @@ DYNAMIC_STRUCT_BEGIN(netlink_kernel_cfg)
 DEFINE_MEMBER(netlink_kernel_cfg, groups)
 DEFINE_MEMBER(netlink_kernel_cfg, flags)
 DEFINE_MEMBER(netlink_kernel_cfg, input)
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0)
 DEFINE_MEMBER(netlink_kernel_cfg, cb_mutex)
+#endif
 DEFINE_MEMBER(netlink_kernel_cfg, bind)
 DEFINE_MEMBER(netlink_kernel_cfg, unbind)
 #if LINUX_VERSION_CODE < KERNEL_VERSION(6, 1, 0)
diff --git a/kernel/hook/lsm_hook.c b/kernel/hook/lsm_hook.c
index d9894a7..f202b64 100644
--- a/kernel/hook/lsm_hook.c
+++ b/kernel/hook/lsm_hook.c
@@ -178 +178,8 @@ static int ksu_task_fix_setuid(struct cred *new, const struct cred *old, int fl
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+static const struct lsm_id ksu_lsmid = {
+    .name = "ksu",
+    .id = LSM_ID_UNDEF,
+};
+#endif
+
 static struct security_hook_list ksu_hooks[] = {
@@ -190,2 +197,4 @@ void __init ksu_lsm_hook_init(void)
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0)
-    security_add_hooks(ksu_hooks, ARRAY_SIZE(ksu_hooks), "ksu");
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+    security_add_hooks(ksu_hooks, ARRAY_SIZE(ksu_hooks), &ksu_lsmid);
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0)
+    security_add_hooks(ksu_hooks, ARRAY_SIZE(ksu_hooks), "ksu");
